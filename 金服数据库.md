```sql
create table ACCT_PAYMENT_SCHEDULE 还款计划表
(
    SERIALNO              VARCHAR2(32) not null comment 主键
    OBJECTNO              VARCHAR2(32), 关联对象id(贷款id ACCT_LOAN表SERIALNO)
    SEQID                 NUMBER, 期次
    PAYDATE               VARCHAR2(10),
    PAYTYPE               VARCHAR2(10),
    INTEDATE              VARCHAR2(10),
    PAYPRINCIPALAMT       NUMBER(20, 2) default 0,
    PAYINTEAMT            NUMBER(20, 2) default 0,
    PRINCIPALBALANCE      NUMBER(20, 2) default 0,
    ACTUALPAYPRINCIPALAMT NUMBER(20, 2) default 0,
    ACTUALPAYINTEAMT      NUMBER(20, 2) default 0,
    FINISHDATE            VARCHAR2(10),
    REMARK                VARCHAR2(80),
    PAYFINEAMT            NUMBER(20, 2) default 0,
    ACTUALPAYFINEAMT      NUMBER(20, 2) default 0,
    PAYCOMPDINTEAMT       NUMBER(20, 2) default 0,
    ACTUALPAYCOMPDINTEAMT NUMBER(20, 2) default 0,
    OBJECTTYPE            VARCHAR2(32),
    AUTOPAYFLAG           VARCHAR2(10),
    SETTLEDATE            VARCHAR2(10),
    HOLIDAYINTEDATE       VARCHAR2(10),
    GRACEINTEDATE         VARCHAR2(10),
    WAIVECOMPDINTEAMT     NUMBER(20, 2) default 0,
    WAIVEFINEAMT          NUMBER(20, 2) default 0,
    WAIVEINTEAMT          NUMBER(20, 2) default 0,
    WAIVEPRINCIPALAMT     NUMBER(20, 2) default 0,
    FIXPRINCIPALAMT       NUMBER(20),
    FIXINSTALLMENTAMT     NUMBER(20),
    RELATIVEOBJECTTYPE    VARCHAR2(32),
    RELATIVEOBJECTNO      VARCHAR2(32), 
    SEQIDTRANSFER         NUMBER,
    PAYINTEAMTTRANSFER    NUMBER(20, 2),
    CREATDATE             VARCHAR2(10)
)
```

每笔贷款有多项费用需要支付, 

每期都有 本金, 利息, 手续费, 居间服务费等等

```sql
create table BATCH_LAS_CORE
(
    INPUTDATE           VARCHAR2(10),
    SERIALNO            VARCHAR2(32) not null, 还款序列号
    OBJECTNO            VARCHAR2(32) not null, 贷款id ACCT_LOAN表SERIALNO
    OBJECTTYPE          VARCHAR2(32) not null,
    DEDUCTSERIALNO      VARCHAR2(32) not null,
    CURRENCY            VARCHAR2(10),
    BATCHTRANSTYPE      VARCHAR2(10),
    PAYAMOUNT           NUMBER(20, 2),
    DEDUCTTYPE          VARCHAR2(10),
    ACCOUNTINGORGID     VARCHAR2(32),
    DIGEST              VARCHAR2(80),
    BATCHNO             VARCHAR2(32),
    BTYPE               VARCHAR2(6),
    HDAY                VARCHAR2(6),
    PAYAMT              NUMBER(24, 6),
    STATUS              VARCHAR2(10),
    FAILPAYAMT          NUMBER(24, 6),
    TRANSSERIALNO       VARCHAR2(32),
    STAYAMOUNT          NUMBER(24, 6),
    RET_NO              NUMBER(20),
    RET_MSG             VARCHAR2(512),
    ISDEDUCTFLAG        VARCHAR2(10),
    REFER_DATE          VARCHAR2(20),
    FAILDETAIL          VARCHAR2(1000),
    SEND_FLAG           VARCHAR2(6),
    BANK_CODE           VARCHAR2(100),
    BANK_NO             VARCHAR2(100),
    PAY_CHANNEL         NUMBER(10),
    FAIL_CODE           VARCHAR2(100),
    PROVIDER_PAYMENT_ID VARCHAR2(100),
    WITHHOLD_TYPE       NUMBER(2) default 0,
    HOLDREASON          VARCHAR2(300),
    HOLDTYPE            VARCHAR2(10),
    PROVIDER_PARTNER_ID VARCHAR2(30),
    PARTNER_ID          VARCHAR2(50),
    CREATE_USER         VARCHAR2(50),
    MAIN_PAY_CHANNEL    VARCHAR2(10),
    SUB_PAY_CHANNEL     VARCHAR2(10)
)
```





